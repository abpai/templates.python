# Copy to .env for local overrides; environment variables take precedence.
# Invalid values fail validation. Other applications' .env keys are ignored.

# DEBUG, INFO, WARNING, ERROR, CRITICAL (case-insensitive)
LOG_LEVEL=INFO

# console (colors only on a terminal) or json; both write to stderr
LOG_FORMAT=console
